<h1>Your Order</h1>
<table>
<tr>
<th>Quantity</th>
<th>Item</th>
</tr>
<%if @order%>
    <% @order.each do |order_item| %>
    <tr>
        <td><%= order_item.quantity %></td>
        <td><%= order_item.name %></td>
    </tr>
    <%end%>
    <tr>
        <th>Total</th>
        <th><%= "$%.2f" % @order_total %></th>
    </tr>
<%end%>
</table>
